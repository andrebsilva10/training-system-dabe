<div class="container col-11 col-md-9" id="form-container">
    <div class="d-flex justify-content-between">
        <h2>Editar Treinamento</h2>
    </div>

    <?php $simpleForm->for($training, "/trainings/{$training->getId()}", 'PUT', function ($form) { ?>
        <?= $form->inputFor('name', 'Nome') ?>
        <?= $form->submit('Salvar') ?>
    <?php }) ?>
</div>
